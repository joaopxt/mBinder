export default interface CollectionCard {
  id: string;
  name: string;
  typeLine: string;
  image: string;
}

export interface PasseCard {
  id: string;
  name: string;
  image: string;
  quantity: number;
}

export interface WantCard {
  id: string;
  name: string;
  image: string;
  quantity: number;
}
